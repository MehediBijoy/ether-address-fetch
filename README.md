# Web3 wallet address fetch
navigate the project root directory and command `yarn install` or `npm install`

### Ether address fetching

```bash
yarn start:ether
```

### Binance address fetching

```bash
yarn start:binance
```
